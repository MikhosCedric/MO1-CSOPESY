Act as an expert systems programmer. I need you to implement a First-Come-First-Serve (FCFS) CPU scheduler for my existing OS emulator project. 

Below are the strict specifications, architecture requirements, and formatting rules. Ensure the code is robust and meets all these criteria perfectly.

### 1. Architecture & Concurrency
* Implement a multi-threaded approach: 1 dedicated thread for the Scheduler, and 1 thread for each CPU worker.
* Declare exactly 4 cores (4 CPU worker threads) for the scheduler.
* Processes must automatically move from the "running" state to the "finished" state without any user intervention.

### 2. Core Features & Commands
* **FCFS Scheduling**: Processes must be executed in the exact order they were created.
* **`print` command behavior**: When a process executes a print command, it must log this to an external text file. 
    * Every process generates its own unique text file (e.g., 10 processes = 10 text files).
    * This feature should be modular so it can be easily disabled later for performance testing.
* **`exit` command**: Safely terminates the emulator and joins all threads.

### 3. Exact UI Formatting (`screen -ls`)
When the user types the `screen -ls` command, the console output MUST match this exact format and spacing:

--------------------------------------------------
Running processes:
process05   (01/18/2024 09:15:22AM)     Core: 0     1235 / 5876
process06   (01/18/2024 09:17:22AM)     Core: 1     3 / 5876
process07   (01/18/2024 09:17:45AM)     Core: 2     9 / 1000
process08   (01/18/2024 09:18:58AM)     Core: 3     12 / 80

Finished processes:
process01   (01/18/2024 09:00:21AM)     Finished    5876 / 5876
process02   (01/18/2024 09:00:22AM)     Finished    5876 / 5876
process03   (01/18/2024 09:00:42AM)     Finished    1000 / 1000
process04   (01/18/2024 09:00:53AM)     Finished    80 / 80
--------------------------------------------------
*Note: Ensure the timestamp uses MM/DD/YYYY HH:MM:SSAM/PM format.*

### 4. Exact Text File Logging Format
Inside the generated text file for each process (e.g., for "screen_01"), the content MUST match this exact format:

Process name: screen_01
Logs:

(08/06/2024 09:15:22AM) Core:0 "Hello world from screen_01!"
(08/06/2024 09:15:28AM) Core:0 "Hello world from screen_01!"
(08/06/2024 09:32:09AM) Core:1 "Hello world from screen_01!"

*Note: The timestamp indicates exactly when the CPU executed it, and the Core ID indicates which of the 4 cores executed that specific line.*

### 5. Automated Test Case Routine
Please include a test script or initialize routine upon startup that does the following to test the implementation:
1. Automatically creates 10 processes upon the start of the OS emulator.
2. Each process should have exactly 100 print commands.
3. Allow me to periodically type `screen -ls` every 1-2 seconds to watch them shift from "running" to "finished".